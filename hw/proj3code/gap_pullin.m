% [u,V] = gap_pullin(pgap)
%
% Compute the pull-in displacement and voltage for the given
% structure.
%
% Inputs:
%    pgap: Structure returned by gap_setup
%
% Outputs:
%    u: Displacement vector at pull-in
%    V: Pull-in voltage
%
function [u,V] = gap_pullin(pgap)

